# Legacy Scripts

These scripts are archived from early ContextRAG iterations and remain available
for historical reference. The canonical implementations live in
`src/contextrag/legacy/`.

Run from the repo root:

```bash
python -m contextrag.legacy.similarity --help
python -m contextrag.legacy.category_assignment
python -m contextrag.legacy.category_extraction
```
